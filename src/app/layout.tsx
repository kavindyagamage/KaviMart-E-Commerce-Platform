import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Header } from "@/components/layout/Header";
import { Footer } from "@/components/layout/Footer";
const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "KaviMart - Sri Lanka's Premium Fashion Destination",
    template: "%s | KaviMart"
  },
  description: "Shop the latest fashion trends for Men, Women, and Kids at KaviMart. Discover premium quality clothing, footwear, and accessories with nationwide delivery.",
  keywords: ["fashion", "clothing", "Sri Lanka", "KaviMart", "men's fashion", "women's fashion", "online shopping"],
  authors: [{ name: "KaviMart Team" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased flex flex-col min-h-screen`}
      >
        <Header />
        <main className="flex-1">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
